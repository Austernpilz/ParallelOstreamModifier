class TaskFlock
{
  void enqueue_task(unsigned char* c, size)
  void enqueue_task()
  void enqueue_task()
  std::vector<ThreadWorker> BeeReady;
  std::deque<ThreadTask> task_que_;
  uint32_t id;
}

class TaskCache
{
  public:

  private:
    AFlockOfThreads* CrowControl;
    std::deque<ThreadFutures> solved_que_;
}